spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/CatOwnerDB
    username: Cat
    password: Owner
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect